import { SolutionsPage, solutionsMetadata } from '@/components/pages/solutions'

export const metadata = solutionsMetadata('en')

export default function Page() { return <SolutionsPage locale='en' /> }
